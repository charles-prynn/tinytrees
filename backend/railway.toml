[build]
builder = "DOCKERFILE"

[deploy]
preDeployCommand = "sh -lc '/app/migrate -path /app/migrations -database \"$DATABASE_URL\" up'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
